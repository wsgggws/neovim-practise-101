## 61. 跳转到匹配括号

```python
def hello(name, age):
    if name:
        print(f"Hello {name}")
        return True
    return False
```

=> 在各个括号之间快速跳转

## 62. 跳转到函数定义

```python
result = calculate(10, 20)

def calculate(a, b):
    return a + b
```

=> 从调用处跳转到定义处

## 63. 跳转到下一个错误

```python
def hello()  # 缺少冒号
    print("hello")
    return True  # 缩进错误
```

=> 快速跳转到语法错误位置
