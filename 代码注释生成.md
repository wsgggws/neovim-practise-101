# 代码注释生成 (96-97)

> ⭐⭐ 中等 | 文档生成

## 96. 生成函数文档

**初始状态**：

```python
def calculate_total(price, quantity, discount):
    return price * quantity * (1 - discount)
```

**操作提示**：

```vim
" 使用 neogen 插件
<leader>gf
" 或手动添加 docstring
```

**目标状态**：

```python
def calculate_total(price, quantity, discount):
    """
    Calculate the total price after discount.

    Args:
        price: Unit price
        quantity: Number of items
        discount: Discount rate (0-1)

    Returns:
        Total price after discount
    """
    return price * quantity * (1 - discount)
```

---

## 97. 生成类文档

**初始状态**：

```python
class User:
    def __init__(self, name, email):
        self.name = name
        self.email = email
```

**操作提示**：

```vim
" 光标在 class 上
<leader>gc
```

**目标状态**：

```python
class User:
    """User model class."""

    def __init__(self, name, email):
        """
        Initialize a User instance.

        Args:
            name: User's name
            email: User's email address
        """
        self.name = name
        self.email = email
```

---

## 注释生成命令

| 操作     | 命令         | 插件   |
| -------- | ------------ | ------ |
| 函数文档 | `<leader>gf` | neogen |
| 类文档   | `<leader>gc` | neogen |
| 手动添加 | `I"""<Esc>`  |        |
